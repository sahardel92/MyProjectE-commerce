
{% extends 'base.html.twig' %}

{% block title %}Checkout - LilyShop{% endblock %}

{% block body %}
<div class="container my-5">
  <div class="row">
    <!-- 📝 Formulaire livraison -->
    <div class="col-md-8">
      <div class="card shadow-sm p-4">
        <h2 class="mb-4">📦 Informations de livraison</h2>

        {{ form_start(form) }}
          <div class="row g-3">
            {{ form_widget(form) }}
          </div>

          <div class="mt-4 text-end">
            <button type="submit" class="btn btn-primary btn-lg">
              ✅ Continuer
            </button>
          </div>
        {{ form_end(form) }}
      </div>
    </div>

    <!-- 📊 Résumé -->
    <div class="col-md-4">
      <div class="card shadow-sm p-4">
        <h4 class="mb-3">🧾 Résumé</h4>

        <div class="d-flex justify-content-between mb-2">
          <span>Produits</span>
          <strong id="product-total">{{ total }} €</strong>
        </div>

        <div class="d-flex justify-content-between mb-2">
          <span>Frais de livraison</span>
          <span id="shipping-cost" class="text-muted">--</span>
        </div>

        <hr>

        <div class="d-flex justify-content-between fs-5">
          <span>Total à payer</span>
          <strong id="grand-total" class="text-success">{{ total }} €</strong>
        </div>
      </div>
    </div>
  </div>

  <!-- ⭐ Suggestions produits -->
  <div class="row mt-5">
    <h3 class="mb-4">🔥 Produits que vous pourriez aimer</h3>

    <div class="col-md-3">
      <div class="card shadow-sm">
        <img src="https://via.placeholder.com/200" class="card-img-top" alt="Produit 1">
        <div class="card-body text-center">
          <h6 class="card-title">Produit Exemple 1</h6>
          <p class="text-success fw-bold">29,99 €</p>
          <a href="#" class="btn btn-outline-primary btn-sm">Ajouter au panier</a>
        </div>
      </div>
    </div>

    <div class="col-md-3">
      <div class="card shadow-sm">
        <img src="https://via.placeholder.com/200" class="card-img-top" alt="Produit 2">
        <div class="card-body text-center">
          <h6 class="card-title">Produit Exemple 2</h6>
          <p class="text-success fw-bold">49,99 €</p>
          <a href="#" class="btn btn-outline-primary btn-sm">Ajouter au panier</a>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card shadow-sm">
        <img src="https://via.placeholder.com/200" class="card-img-top" alt="Produit 2">
        <div class="card-body text-center">
          <h6 class="card-title">Produit Exemple 3</h6>
          <p class="text-success fw-bold">30,99 €</p>
          <a href="#" class="btn btn-outline-primary btn-sm">Ajouter au panier</a>
        </div>
      </div>
    </div>
    <!-- tu peux dupliquer encore selon besoin -->
  </div>
</div>

<script>
  // ⚡ Script sans jQuery -> Fetch API
  document.addEventListener("DOMContentLoaded", () => {
    const citySelector = document.querySelector("#order_city");
    const productTotal = parseFloat("{{ total }}");
    const shippingCostEl = document.getElementById("shipping-cost");
    const grandTotalEl = document.getElementById("grand-total");

    async function updateShipping(cityId) {
      try {
        const response = await fetch(`/city/${cityId}/shipping/cost`);
        const data = await response.json();

        if (data.status === 200) {
          const shipping = parseFloat(data.content);

          shippingCostEl.textContent = `${shipping.toFixed(2)} €`;
          grandTotalEl.textContent = `${(productTotal + shipping).toFixed(2)} €`;
        }
      } catch (error) {
        shippingCostEl.textContent = "Erreur...";
      }
    }

    // Charger par défaut
    if (citySelector) {
      updateShipping(citySelector.value);

      citySelector.addEventListener("change", (e) => {
        updateShipping(e.target.value);
      });
    }
  });
</script>
{% endblock %}

