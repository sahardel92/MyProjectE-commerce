<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title>{% block title %}LilyShop{% endblock %}</title>

        <!-- Bootstrap CSS -->
        <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">

        <!-- 🌟 FontAwesome (icônes étoiles, panier, etc.) -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
                crossorigin="anonymous"></script>

        <!-- Icône -->
        <link rel="icon"
              href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 128 128'>
              <text y='1.2em' font-size='96'>⚫️</text>
              <text y='1.3em' x='0.2em' font-size='76' fill='#fff'>sf</text></svg>">

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        <!-- 🎨 Styles personnalisés -->
        <style>
            /* SLIDER PRINCIPAL */
            .hero-img {
                height: 520px;
                width: 100%;
                object-fit: cover;
                border-radius: 20px;
                background: #fff;
            }

            .carousel-inner {
                border-radius: 20px;
                overflow: hidden;
            }

            .carousel-item {
                transition: opacity 0.8s ease-in-out;
            }

            /* Ajustement général page */
            body {
                background-color: #f4f4f4;
            }
        </style>

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>

    <body>
        {% block nav %}
            {% include 'layouts/nav.html.twig' %}
        {% endblock %}

        <main class="mt-4">
            {% block body %}
            {% endblock %}
        </main>
    </body>
</html>
